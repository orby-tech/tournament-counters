import { createSlice } from "@reduxjs/toolkit";

export const baseEditorSlice = createSlice({
  name: "baseEditor",
  initialState: {},
  reducers: {},
});

export const {} = baseEditorSlice.actions;
export default baseEditorSlice.reducer;
